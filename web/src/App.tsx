import Preview from './dev/Preview'

// The real workspace layout lands with PHY-115; until then the app renders
// the design-system preview used for visual verification (PHY-130).
export default function App() {
  return <Preview />
}
